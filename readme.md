# Docker Run & Log Monitoring Guide

rest api service untuk menggunakan docker : 
baik menggunakan `docker run` maupun `docker compose`.

---

## 1. Menjalankan Container (Foreground)

Menjalankan container dan langsung menampilkan log di terminal.

```bash
docker compose up

```bash 
docker ps 
check semua service yang aktive 


```bash

docker ps -a

